import { motion } from 'framer-motion';

export const GlassBackground = () => {
  return (
    <div className="fixed inset-0 overflow-hidden pointer-events-none">
      {/* Empty container for potential future background elements */}
    </div>
  );
};